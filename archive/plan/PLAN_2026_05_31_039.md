# PLAN_2026_05_31_035

## Problem Statement

The codebase has 9 identified issues from architecture + code quality analysis: 3 inline imports that should be top-level, 1 unnecessary `Any` return type, 1 misnamed module boundary (theme in hooks/), 1 DRY violation (uuid4 short-ID duplicated 5x), 1 unconventional import pattern (from `__init__.py`), 1 typing modernization opportunity, and stale ARCHITECTURE.md.

## Goals

- Clean up all 3 inline imports to top-level
- Fix `get_rooms()` return type to remove `from typing import Any`
- Move `ThemeProvider` from `hooks/` to `context/`
- Extract shared `short_id()` helper for uuid4 truncation
- Move `FRONTEND_DIST_DIR` to `config.py`
- Modernize `Optional[str]` → `str | None`
- Sync ARCHITECTURE.md to match current code

## Non-Goals

- No behavior changes, bug fixes, or new features
- No frontend restructuring beyond `ThemeProvider` move
- No database changes

## Success Metrics

- All existing tests pass (31 backend, 96 frontend)
- TSC clean
- `npm run build` succeeds
- Zero `from typing import Any` in app/
- Zero inline imports in routes.py and matchmaking.py
- ARCHITECTURE.md reflects current code

## Batches

### Batch 1: Fix inline imports (P1)
- `routes.py:123` — `from .matchmaking import find_match` → top-level
- `routes.py:180` — `from .qr_utils import generate_qr_data_uri` → top-level
- `matchmaking.py:46` — `MATCH_EXPIRY_SECONDS` merge with existing `from .config import DB_PATH` at line 15
- No test changes needed

### Batch 2: Fix get_rooms() return type (P1)
- `routes.py:49` — `-> Any` → `-> list[dict[str, str]] | JSONResponse`
- `routes.py:17` — remove `from typing import Any`
- No test changes needed

### Batch 3: Move FRONTEND_DIST_DIR to config.py (P2)
- `config.py` — add `FRONTEND_DIST_DIR` alongside `BASE_DIR`
- `__init__.py` — import `FRONTEND_DIST_DIR` from config instead of computing inline
- `routes.py:22` — update import from `from . import FRONTEND_DIST_DIR` to `from .config import FRONTEND_DIST_DIR`
- No test changes needed

### Batch 4: Extract short_id() helper (P2)
- Create shared utility `app/helpers.py`
- Replace 5 instances: routes.py:37,42,61,75 + matchmaking.py:47
- No test changes needed

### Batch 5: Move ThemeProvider to context/ (P1)
- Move `useTheme.tsx` content to `context/useTheme.tsx`
- Update `App.tsx:10` import from `@/hooks/useTheme` to `@/context/useTheme`
- No behavioral change

### Batch 6: Modernize Optional typing (P2)
- `state.py:20,24` — `Optional[str]` → `str | None`
- `state.py:15` — remove `Optional` from import (keep `TypedDict`)
- `schemas.py:20-22` — `Optional[str]` → `str | None`
- `schemas.py:14` — remove `from typing import Optional`
- No test changes needed

### Batch 7: Sync ARCHITECTURE.md (P2)
- Project structure tree: add `websocket_handler.py`, update routes.py description
- `routes.py` section: remove WS endpoint from title, description, and function list
- Add `websocket_handler.py` module section
- Remove `broadcast_to_room` from connection_manager.py docs
- No test changes needed

### Files to Modify

| Batch | Action | File |
|-------|--------|------|
| 1 | MODIFY | `app/routes.py` |
| 1 | MODIFY | `app/matchmaking.py` |
| 2 | MODIFY | `app/routes.py` |
| 3 | MODIFY | `app/config.py` |
| 3 | MODIFY | `app/__init__.py` |
| 3 | MODIFY | `app/routes.py` |
| 4 | CREATE | `app/helpers.py` |
| 4 | MODIFY | `app/routes.py` |
| 4 | MODIFY | `app/matchmaking.py` |
| 5 | CREATE | `frontend/src/context/useTheme.tsx` |
| 5 | MODIFY | `frontend/src/App.tsx` |
| 6 | MODIFY | `app/state.py` |
| 6 | MODIFY | `app/schemas.py` |
| 7 | MODIFY | `docs/ARCHITECTURE.md` |

### Testing Strategy

- Run full backend test suite after each batch: `uv run python tests/test_app.py`
- Run TSC after each batch: `cd frontend && npx tsc --noEmit`
- Run frontend unit tests: `cd frontend && npm test`
- Final run: all 3 suites + `npm run build`
