# PLAN_2026_05_31_036

## Problem Statement

After implementing 7 batches of code improvements (inline→top-level imports, `Any`→concrete types, `FRONTEND_DIST_DIR`→`config.py`, `short_id()`→`helpers.py`, ThemeProvider relocation, `Optional[str]`→`str | None`, ARCHITECTURE.md sync), 5 documentation files have stale content, missing sections, and correctness issues that need to be synced. A previous `update-docs` phase applied all changes at once without per-edit approval — the user reverted everything. This plan re-applies the same approved changes with structured batch approval.

## Goals & Non-Goals

### Goals
- Update `docs/README.md` with: new Documentation section, test command fix, expanded Troubleshooting, Deployment cleanup (remove `ENV=production` claim), stale path fix
- Update `docs/SPECIFICATIONS.md` with: shortened Architecture Overview summary + Product Decisions table
- Fix `AGENTS.md` frontend test glob: `*` → `**/*` for recursive matching
- Update `refs/PROJECT_BEST_PRACTICES.md`: add §1.6, §1.7, §1.8, §12.1; fix duplicate line in §1.5
- Update `refs/AGENT_SETUP.md`: bump last-verified date; add opencode.json permission block reference
- Verify no regressions: all backend tests (31), frontend tests (96), `tsc --noEmit`, `npm run build`

### Non-Goals
- No code changes (Python, TypeScript, CSS, configs)
- No behavioral changes, bug fixes, or new features
- No new documentation files — only edits to existing 5 files
- No commit or push — doc updates only

## Success Metrics

1. All 5 document files updated with approved changes — verified by reading each file after edits
2. Full test suite passes: `uv run python tests/test_app.py` (31 tests), `cd frontend ; npm test` (96 tests)
3. Type-check passes: `cd frontend ; npx tsc --noEmit`
4. Build succeeds: `cd frontend ; npm run build`
5. No stale patterns remain (grep check for old content across docs)

## Technical Approach

Surgical edits using `oldString→newString` replacements via the `edit` tool. Each change is independently verifiable. Apply one document per batch, show the user the diff, get sign-off before applying.

## Implementation Plan

### Files to Create, Modify, or Remove

| Action | File | Scope |
|--------|------|-------|
| Edit | `docs/README.md` | 5 surgical edits |
| Edit | `docs/SPECIFICATIONS.md` | 2 surgical edits |
| Edit | `AGENTS.md` | 1 surgical edit |
| Edit | `refs/PROJECT_BEST_PRACTICES.md` | 5 surgical edits |
| Edit | `refs/AGENT_SETUP.md` | 2 surgical edits |

### Architecture & Design Decisions

- **No structural changes** — each file's existing section structure is preserved
- **Surgical edits only** — no file rewrites, preserving all existing context not explicitly targeted
- **One doc per batch** with user sign-off before applying, per the User Approval Gate rule

### Testing Strategy

- Backend: `uv run python tests/test_app.py` (31 tests) — doc-only changes should not affect backend
- Frontend unit: `cd frontend ; npm test` (96 tests)
- Type-check: `cd frontend ; npx tsc --noEmit`
- Build: `cd frontend ; npm run build`
- Verification: read each edited file to confirm correct application
- Stale pattern audit: grep for removed content across all docs

### Task Breakdown

#### Batch 1: `docs/README.md`

**What**: Apply 5 surgical edits:
1. Add "Documentation" section entry in Table of Contents + section body with links to `docs/ARCHITECTURE.md`, `docs/SPECIFICATIONS.md`, `docs/DESIGN_SPEC.md`
2. Fix test command section for correctness
3. Expand Troubleshooting section with additional entries
4. Remove `ENV=production` note in Deployment section
5. Fix stale path reference (wherever the stale path appears)

**Files**: `docs/README.md`
**Dependencies**: None
**Tests**: Backend (31), Frontend (96), `tsc --noEmit`, `npm run build`
**Verification**: Read file; check table of contents consistency; confirm all 5 changes applied

#### Batch 2: `docs/SPECIFICATIONS.md`

**What**: Apply 2 surgical edits:
1. Shorten Architecture Overview (lines 95-101) — condense the 3-paragraph narrative into a single summary paragraph that keeps the Frontend/Backend/Data Model gist and links to the full technology table below
2. Add Product Decisions table

**Files**: `docs/SPECIFICATIONS.md`
**Dependencies**: None
**Tests**: Backend (31), Frontend (96), `tsc --noEmit`, `npm run build`
**Verification**: Read file; confirm Architecture Overview is shorter but still has the three key concepts + link; confirm Product Decisions table exists

#### Batch 3: `AGENTS.md`

**What**: Apply 1 surgical edit:
1. Line 192: Change `frontend/tests/*.test.{ts,tsx}` → `frontend/tests/**/*.test.{ts,tsx}` (recursive glob)

**Files**: `AGENTS.md`
**Dependencies**: None
**Tests**: Backend (31), Frontend (96), `tsc --noEmit`, `npm run build`
**Verification**: Read line 192 — confirm `**/*` pattern is in place

#### Batch 4: `refs/PROJECT_BEST_PRACTICES.md`

**What**: Apply 5 surgical edits:
1. Add §1.6 (new modularization principle) after §1.5
2. Add §1.7 (new principle) after §1.6
3. Add §1.8 (new principle) after §1.7
4. Add §12.1 (new section) after the last existing section
5. Fix §1.5 duplicate line — last line repeats "Business logic → `matchmaking.py`" — remove one

**Files**: `refs/PROJECT_BEST_PRACTICES.md`
**Dependencies**: None
**Tests**: Backend (31), Frontend (96), `tsc --noEmit`, `npm run build`
**Verification**: Read file; confirm §1.6, §1.7, §1.8, §12.1 exist; confirm duplicate removed from §1.5

#### Batch 5: `refs/AGENT_SETUP.md`

**What**: Apply 2 surgical edits:
1. Line 3: Change `> **Last verified:** 2026-05-30` → `> **Last verified:** 2026-05-31`
2. Add opencode.json permission block reference in Troubleshooting section

**Files**: `refs/AGENT_SETUP.md`
**Dependencies**: None
**Tests**: Backend (31), Frontend (96), `tsc --noEmit`, `npm run build`
**Verification**: Read file; confirm date updated; confirm permission block reference present

### Success Criteria

- All 5 files edited with all approved changes applied
- All batch verifications pass: backend (31), frontend (96), `tsc --noEmit`, `npm run build`
- No stale patterns (grep for old content confirms removal)
- User signed off on each batch before application

## Open Questions & Risks

- **Risk**: Doc content may conflict with code-level structural tests (e.g., `test_js_modules.py` checking file existence). Mitigation: run full test suite after each batch.
- **Risk**: `AGENTS.md` edit might trigger structural validation tests if they check glob patterns. Mitigation: run all tests; fix test if brittle.

---

## Grill Outcomes

### Dimension: User Approval Gate
- **Decision**: Every proposed doc edit must be shown to the user with exact old→new diff before applying
- **Rationale**: Previous `update-docs` phase applied all changes at once without per-edit approval — user reverted everything
- **Impact**: Implementation proceeds one batch at a time, each requiring user sign-off via `question` tool

### Dimension: Scope Boundary
- **Decision**: Documentation-only — no code changes, no new files, no behavioral modifications
- **Rationale**: All code improvements (Batches 1–7) are already committed; only remaining items are doc sync
- **Impact**: Implementation touches exactly 5 existing files with surgical edits

### Dimension: Change Content per Document
- **Decision**: Changes per document as defined in Task Breakdown above — exact content sourced from conversation-context approval walkthrough
- **Rationale**: User reviewed and approved each document's changes in the re-analysis phase
- **Impact**: Implement-plan uses conversation context for exact oldString→newString values, not re-analysis

## Readiness Gate Results

| Gate | Pass? | Notes |
|------|-------|-------|
| Context | ✅ | Plan cites all 5 target files, current state verified via `Read`, architecture from ARCHITECTURE.md |
| Assumptions | ✅ | No unresolved assumptions — all changes explicitly approved by user in walkthrough |
| Edge Cases | ✅ | Surgical edits preserve untargeted content; test suite catches regressions; stale-pattern grep as final check |
| Clarity | ✅ | Each batch has explicit scope, files, verification steps; one document per batch |
| Soundness | ✅ | Plan satisfies intended function: all stale doc content corrected without affecting code behavior |
| Testing | ✅ | Specific commands: backend tests (31), frontend tests (96), `tsc --noEmit`, `npm run build`, read-verification per file |
| Success Criteria | ✅ | All 5 files updated, all tests pass, no stale patterns remain, user signed off per batch |
| Community Coverage | ✅ | 5 documentation files are independent code graph communities — no hidden dependencies; ARCHITECTURE.md (Batch 7) is already updated and not in scope |
