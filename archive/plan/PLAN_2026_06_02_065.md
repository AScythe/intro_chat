# PLAN_2026_06_02_057

## Problem Statement

Tests have drifted from the current codebase. A comprehensive audit of all 30 test files found 24 issues: missing test files for new modules (`useChipSelection.ts`, `random.ts`, `client.ts`), stale mock data (`RoomPage.test.tsx`), missing structural validation entries (5 app modules, 5 frontend files), stale comments referencing outdated patterns, and several untested exported functions (`formatDuration`, `storeData`/`getData`). These gaps reduce test reliability and may hide regressions.

## Goals & Non-Goals

### Goals
- Add test files for all source modules that currently lack coverage
- Fix stale mock data, assertions, and comments across backend + frontend tests
- Update structural validation (`test_app.py`, `test_js_modules.py`) to match actual file listings
- Fix stale summaries and duplicate entries
- All tests pass after every change

### Non-Goals
- No behavioral code changes — tests only
- No architectural changes to test framework or CI
- No new integration/E2E scenarios — unit and structural validation only
- No changes to `tests/test_db.py` (verified accurate)

## Success Metrics

- `useChipSelection.test.ts` exists with tests for toggle, add, handleClick, re-render state stability
- `random.test.ts` exists with tests for generateRandomString and generateUsername
- `client.test.ts` exists with tests for fetchJSON timeout and HTTP error handling
- `format.test.ts` covers both formatTime and formatDuration
- `storage.test.ts` covers storeData and getData in addition to userId functions
- `RoomPage.test.tsx` mocks include `selected` and `is_default` fields
- `test_app.py` file structure lists all 19 app modules (5 added)
- `test_app.py` imports test covers `app.connection_service`
- `test_js_modules.py` console.log scan covers all frontend source files (5 added)
- `test_js_modules.py` summary text accurate (6 hooks, 7 pages)
- `test_js_modules.py` duplicate entry removed
- `test_agent_guidelines.py` updated: Phase 0 Hard Gate → Three-Tier Classification, save-session routing cases added, save-session in UTILITY_SKILLS, PROJECT_ROOT naming fixed
- `test_agent_guidelines.py` UTILITY_SKILLS includes save-session
- `PeoplePageViews.test.tsx` regex tightened
- `useTimer.test.ts` covers getTimeLeft
- `useChatRequest.test.ts` covers theirReady timing

## Technical Approach

Read → edit → verify per batch. Each batch is independently testable. No behavioral code changes.

## Implementation Plan

### Files to Create, Modify, or Remove

| Action | File | Change |
|--------|------|--------|
| Create | `frontend/tests/hooks/useChipSelection.test.ts` | Test toggle/add/handleClick/state stability |
| Create | `frontend/tests/utils/random.test.ts` | Test generateRandomString/generateUsername |
| Create | `frontend/tests/api/client.test.ts` | Test fetchJSON timeout + error handling |
| Modify | `frontend/tests/utils/format.test.ts` | Add formatDuration tests |
| Modify | `frontend/tests/utils/storage.test.ts` | Add storeData/getData tests |
| Modify | `frontend/tests/pages/RoomPage.test.tsx` | Fix mockRooms types |
| Modify | `frontend/tests/hooks/useTimer.test.ts` | Add getTimeLeft test |
| Modify | `frontend/tests/hooks/useChatRequest.test.ts` | Add theirReady timing test |
| Modify | `frontend/tests/components/PeoplePageViews.test.tsx` | Tighten regex |
| Modify | `tests/test_app.py` | Add 5 modules to file structure + connection_service import |
| Modify | `tests/test_js_modules.py` | Add 5 files to scan, fix summary, remove duplicate |
| Modify | `tests/test_agent_guidelines.py` | Fix comment, add routing cases, add UTILITY_SKILLS, fix naming |

### Architecture & Design Decisions

- **Test double discipline**: Stub data for static exports, Spy for timing callbacks, Mock for network requests
- **New test files follow existing conventions**: Vitest for frontend, unittest-like for Python backend
- **No integration/E2E**: All new tests are unit tests

### Testing Strategy

- Run affected test suite(s) after each batch:
  - Batch 1 (backend structural): `py tests/test_app.py` + `py tests/test_js_modules.py`
  - Batch 2 (agent rules): `py tests/test_agent_guidelines.py`
  - Batch 3 (new frontend files): `npm test`
  - Batch 4 (frontend fixes): `npm test`
- Final: full suite: backend, frontend 147/147, TypeScript 0 errors, structural

### Task Breakdown

#### Phase 1: Backend structural validation

1. **Task 1: Add missing app modules to test_app.py structure + import lists**
   - What: Add `connection_service.py`, `handlers.py`, `prompts.py`, `qr_utils.py`, `sample_users.py` to required_files and add connection_service import check
   - Files: `tests/test_app.py`
   - Verification: `uv run python tests/test_app.py` passes

2. **Task 2: Add missing frontend files to test_js_modules.py scan + fix summary**
   - What: Add 5 files to scan list, fix summary hooks+pages counts, remove duplicate entry
   - Files: `tests/test_js_modules.py`
   - Verification: `uv run python tests/test_js_modules.py` passes

#### Phase 2: Backend comments & agent rules

3. **Task 3: Fix stale comments and agent guideline tests**
   - What: Fix comment in test_app.py:354, fix "Phase 0 Hard Gate" → "Three-Tier Classification", add save-session routing cases, add save-session to UTILITY_SKILLS, fix PROJECT_ROOT naming
   - Files: `tests/test_app.py`, `tests/test_agent_guidelines.py`
   - Verification: Both test files pass

#### Phase 3: Frontend missing test files

4. **Task 4: Add useChipSelection.test.ts**
   - What: Test toggle, add, handleClick, state stability on re-render
   - Files: `frontend/tests/hooks/useChipSelection.test.ts` (new)
   - Verification: `npm test` passes

5. **Task 5: Add random.test.ts**
   - What: Test generateRandomString length/characters, generateUsername format
   - Files: `frontend/tests/utils/random.test.ts` (new)
   - Verification: `npm test` passes

6. **Task 6: Add client.test.ts**
   - What: Test fetchJSON timeout, HTTP error (non-ok), and successful fetch
   - Files: `frontend/tests/api/client.test.ts` (new)
   - Verification: `npm test` passes

7. **Task 7: Add formatDuration tests to format.test.ts**
   - What: Test formatDuration with various inputs
   - Files: `frontend/tests/utils/format.test.ts`
   - Verification: `npm test` passes

8. **Task 8: Fix RoomPage.test.tsx mocks**
   - What: Add `selected` and `is_default` to mockRooms
   - Files: `frontend/tests/pages/RoomPage.test.tsx`
   - Verification: `npm test` passes

#### Phase 4: Frontedge coverage & path additions

9. **Task 9: Add storeData/getData tests to storage.test.ts**
   - Files: `frontend/tests/utils/storage.test.ts`
   - Verification: `npm test` passes

10. **Task 10: Add getTimeLeft test to useTimer.test.ts + theirReady to useChatRequest.test.ts**
    - Files: `frontend/tests/hooks/useTimer.test.ts`, `frontend/tests/hooks/useChatRequest.test.ts`
    - Verification: `npm test` passes

11. **Task 11: Tighten PeoplePageViews regex**
    - Files: `frontend/tests/components/PeoplePageViews.test.tsx`
    - Verification: `npm test` passes

#### Phase 5: Final verification

12. **Task 12: Full test suite**
    - What: Backend, frontend, TypeScript, structural
    - Verification: All suites pass

### Success Criteria

- All 24 audit issues resolved
- No backend test regressions
- Frontend 147+ tests pass (adjusted for new additive tests)
- TypeScript 0 errors
- All cross-reference links in docs resolve

## Open Questions & Risks

- The `fetchJSON` test (`client.test.ts`) is read-only since fetch is a browser API; Vitest with jsdom provides `globalThis.fetch` automatically
- The `useChipSelection` test requires rendering inside a React component; testing-library + vitest is already configured

---

## Readiness Gate Results

| Gate | Pass? | Notes |
|------|-------|-------|
| Context | ✅ | All docs/code consulted: test_app.py, test_js_modules.py, test_agent_guidelines.py, all frontend test + source files, api.ts types |
| Assumptions | ✅ | Assumptions confirmed: test-only changes, no behavioral impact, existing test patterns followed for new files |
| Edge Cases | ✅ | Addressed: RoomPage mock completeness, console.log scan coverage, import test coverage for all app modules |
| Clarity | ✅ | No unresolved ambiguities — each task specifies exact files and changes |
| Soundness | ✅ | Plan satisfies intended function: sync all tests to actual codebase state |
| Testing | ✅ | Each batch verifiable via its own test suite; full suite at end |
| Success Criteria | ✅ | Verifiable: file existence checks, type coverage assertions, grep for stale patterns |
| Community Coverage | ✅ | Graphify unavailable (not connected) — test-only changes, no source code communities affected |
