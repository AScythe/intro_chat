# PLAN_2026_06_02_056

## Problem Statement

Documentation has drifted from the actual codebase. Three files (`docs/README.md`, `docs/SPECIFICATIONS.md`, `docs/DESIGN_SPEC.md`) contain stale commands, missing features, or HSL values that don't match the implementation. `docs/ARCHITECTURE.md` was already synced in a prior batch.

## Goals & Non-Goals

### Goals
- Sync `docs/README.md` command references to match actual `package.json` scripts
- Sync `docs/DESIGN_SPEC.md` color system tables to match actual `frontend/src/styles/global.css` HSL values
- Sync `docs/SPECIFICATIONS.md` user-flow description to match actual UI (chat extension behavior, interest selection)
- Verify cross-reference integrity: all `See` links between docs resolve
- All backend + frontend tests continue to pass

### Non-Goals
- No behavioral code changes — documentation-only sync
- No changes to AGENTS.md, AGENT_SETUP.md, PROJECT_BEST_PRACTICES.md, DOCUMENT_GUIDELINES.md (all verified accurate)
- No DESIGN_SPEC.md structural reformatting — only value corrections

## Success Metrics

- `README.md` line 192: `npm run type-check` → `npx tsc --noEmit`
- `SPECIFICATIONS.md` Step 8 updated: remove "chat indefinitely" language
- `SPECIFICATIONS.md` Step 2 updated: add interest/topic selection mention
- `DESIGN_SPEC.md` light + dark HSL values in Color System tables match `global.css`
- `DESIGN_SPEC.md` Design Token Mapping section HSL values updated to match
- All cross-reference links resolve (no dead `See` links)
- Full test suite passes (frontend 147/147, backend all pass, TypeScript 0 errors)

## Technical Approach

Read → edit → verify pattern. Each doc is independently modifiable. No behavioral code changes.

**Decision: sync docs TO code** (code is the executable truth). The design spec was aspirational; the CSS is the actual deployed design.

## Implementation Plan

### Files to Create, Modify, or Remove

| Action | File | Change |
|--------|------|--------|
| Modify | `docs/README.md` | Fix `npm run type-check` → `npx tsc --noEmit` |
| Modify | `docs/SPECIFICATIONS.md` | Step 2: add interest selection; Step 8: fix "indefinitely" wording |
| Modify | `docs/DESIGN_SPEC.md` | Update light + dark HSL/hex values to match global.css |

### Architecture & Design Decisions

- **Code is truth**: All doc values updated to match actual CSS/code, never the reverse
- **One doc per batch**: Each file is an independent edit with no dependencies on other changes
- **No cross-doc dependencies**: Changes to one doc do not affect another

### Testing Strategy

- Run `npx tsc --noEmit` — no TypeScript errors (no code changed)
- Run `uv run python tests/test_app.py` — backend integration tests
- Run `npm test` (frontend Vitest) — frontend unit tests
- Run `uv run python tests/test_js_modules.py` — structural validation

### Task Breakdown

#### Phase 1: Fix README.md

1. **Task 1: Update stale command**
   - What: Change `npm run type-check` to `npx tsc --noEmit`
   - Files: `docs/README.md`
   - Verification: `npx tsc --noEmit` runs (no code changed, still passes)

#### Phase 2: Fix SPECIFICATIONS.md

2. **Task 2: Update user-flow Step 2**
   - What: Add interest/topic selection to profile setup description
   - Files: `docs/SPECIFICATIONS.md`
   - Verification: Step 2 mentions interest selection

3. **Task 3: Update user-flow Step 8**
   - What: Change "or continue indefinitely" to reflect actual extend-or-end behavior
   - Files: `docs/SPECIFICATIONS.md`
   - Verification: Step 8 no longer mentions "indefinitely"

#### Phase 3: Fix DESIGN_SPEC.md

4. **Task 4: Update light-mode HSL/hex values**
   - What: Sync Color System table and Design Token Mapping section to global.css values (background, ring, muted-foreground)
   - Files: `docs/DESIGN_SPEC.md`
   - Verification: All light-mode HSL values match global.css `:root` block

5. **Task 5: Update dark-mode HSL/hex values**
   - What: Sync Color System dark table and Token Mapping to global.css `.dark` block values
   - Files: `docs/DESIGN_SPEC.md`
   - Verification: All dark-mode HSL values match global.css `.dark` block

#### Phase 4: Final verification

6. **Task 6: Run full test suite + cross-reference check**
   - What: Backend tests, frontend tests, TypeScript type check, structural validation
   - Verification: All suites pass, no dead `See` links

### Success Criteria

- `README.md` line 192: `npm run type-check` no longer present
- `SPECIFICATIONS.md`: interest selection mentioned in Step 2, no "indefinitely" in Step 8
- `DESIGN_SPEC.md`: all HSL values match `global.css` (both `:root` and `.dark`)
- Cross-references: running link check shows zero broken `See` links
- Tests: all backend, frontend, structural suites pass

## Open Questions & Risks

- None identified — all changes are straightforward text replacements with no behavioral impact

---

## Grill Outcomes

### Dimension: README.md command reference
- Decision: `npm run type-check` → `npx tsc --noEmit`
- Rationale: The script doesn't exist in `package.json`; `npx tsc --noEmit` is the actual command (from AGENTS.md)
- Impact: Single-line fix in README.md

### Dimension: DESIGN_SPEC.md HSL drift
- Decision: Sync spec values TO actual CSS values
- Rationale: Code is executable truth; CSS is the deployed design
- Impact: ~6 HSL values updated across light/dark mode tables + token mapping

### Dimension: SPECIFICATIONS.md user-flow gaps
- Decision: Add interest selection to Step 2; fix "indefinitely" in Step 8
- Rationale: These reflect actual UI behavior that the spec omits or misstates
- Impact: Two small sentence-level changes

### Dimension: Docs sync direction
- Decision: Docs → Code direction (not Code → Spec)
- Rationale: Specs document reality; reality is the running application
- Impact: No code changes required

## Readiness Gate Results

| Gate | Pass? | Notes |
|------|-------|-------|
| Context | ✅ | All consulted docs/code cited: global.css, package.json, UserInfoPage, ChatPage, all 8 doc files |
| Assumptions | ✅ | Assumptions confirmed: code is truth, doc-only changes, no behavioral impact |
| Edge Cases | ✅ | Addressed: cross-reference integrity check, test suite verification |
| Clarity | ✅ | No unresolved ambiguities — all changes are specific text replacements |
| Soundness | ✅ | Plan satisfies intended function: sync docs to code without behavioral changes |
| Testing | ✅ | Task 6 defines specific verification: backend tests, frontend tests, TS check, cross-reference scan |
| Success Criteria | ✅ | Verifiable: grep for stale string, diff check for HSL values |
| Community Coverage | ✅ | Graph query: docs changes touch only documentation community; no code communities affected |
