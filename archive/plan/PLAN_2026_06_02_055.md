# PLAN_2026_06_02_054 — Architecture & Cleanup (Combined)

## Problem Statement
The codebase has accumulated structural drift (ARCHITECTURE.md out of sync), dead code, duplicate logging config (3 files), missing type hints, late imports, DRY violations (duplicate handler + duplicate frontend types + duplicate chip-selection logic), mixed concerns in matchmaking and routes, silent error swallowing, naming inconsistencies, type erosion, and unprotected global mutable state.

## Goals & Non-Goals

### Goals
Cover **every finding** from both `improve-architecture` and `modularize-and-clean` Phase 1 scans. No item excluded.

### Non-Goals
- No behavior changes, bug fixes, or new features
- No test logic changes (import references only)

## Success Metrics
- Same test counts before and after: frontend 147/147 (25 files), backend all pass, TS clean
- Zero behavioral regression
- All changed lines carry [ARCH] or [CLEANUP] flag

## Technical Approach (6 Batches)

### Batch 1: [CLEANUP] Dead code & duplicate logging

**Files:** `sample_users.py`, `connection_manager.py`, `__main__.py`

| Item | Finding | Change |
|------|---------|--------|
| 1a | `sample_users.py:46` — `SAMPLE_USERS_BY_NAME` zero imports | Remove dict |
| 1b | `connection_manager.py:6` — duplicate `basicConfig()` | Strip to `logger = getLogger(__name__)` |
| 1c | `__main__.py:6` — duplicate `basicConfig()` | Strip to `logger = getLogger(__name__)` |
| 1d | `sample_users.py:6` — duplicate `basicConfig()` | Strip to `logger = getLogger(__name__)` |

### Batch 2: [CLEANUP] Type hints, imports & error handling

**Files:** `helpers.py`, `__init__.py`, `database.py`, `routes_api.py`

| Item | Finding | Change |
|------|---------|--------|
| 2a | `helpers.py:14,22,30` — `db` param missing type hint | Add `db: aiosqlite.Connection` |
| 2b | `__init__.py:21-50` — late imports in module body | Move all imports to top of file |
| 2c | `__init__.py:35-40` — 404 handler inline | Extract into `app/handlers.py` + import in `__init__.py` |
| 2d | `database.py:44-51,84-101` — silent `except OperationalError: pass` | Use `PRAGMA table_info` to check column existence before ALTER |
| 2e | `routes_api.py:278-279` — `or {}` loses `UserData` type | Replace with `store.active_users.get(...) or UserData(...)` |

### Batch 3: [CLEANUP] DRY violations & naming

**Files:** `routes_api.py`, `OrganizeEventPage.tsx`, `demoData.ts`, `PersonCard.tsx`, `UserInfoPage.tsx`, `useDemoMode.ts`, `pages/PeoplePage.tsx`

| Item | Finding | Change |
|------|---------|--------|
| 3a | `routes_api.py:50-53` — fallback query identical to `get_rooms_for_event` | Remove dead fallback (helper already does same query) |
| 3b | `OrganizeEventPage.tsx:51-83` — `addRoom`/`addTopic`, `toggleRoom`/`toggleTopic` identical pattern | Extract `useChipSelection` hook to `hooks/useChipSelection.ts` |
| 3c | `demoData.ts:4-10` + `types/api.ts:37-43` — `SamplePerson` vs `SampleUserData` identical | Remove `SamplePerson`, import `SampleUserData` from `api.ts` everywhere |
| 3d | `UserInfoPage.tsx:29` — `saved` reads like past participle | Rename to `isSaved` |
| 3e | `useDemoMode.ts` — name under-represents scope (5 methods, not just toggle) | Rename to `useDemoSimulation` + update all imports |
| 3f | `OrganizeEventPage.tsx:14-19` — `ConfigItem` internal to component | Export from `types/api.ts` or merge with `Room`/`Topic` |

### Batch 4: [ARCH] Architecture improvements — split mixed concerns

**Files:** `matchmaking.py`, `routes_api.py`, `tasks.py`

| Item | Finding | Change |
|------|---------|--------|
| 4a | `matchmaking.py:41-78` — `create_match()` does DB insert + state mutation + waiting queue manipulation + WS broadcast | Split into `persist_match()` (DB), `update_match_state()` (in-memory), `notify_match_found()` (WS) |
| 4b | `routes_api.py:288-317` — `exchange_connection()` does route handler + state logic + WS broadcast | Extract connection-exchange logic into `connection_service.py` |
| 4c | `tasks.py:10-23` — `cleanup_expired_matches()` mixes infinite loop + sleep + lock + dict mutation | Extract `find_expired_matches()` pure function, keep loop in `cleanup_expired_matches()` |

### Batch 5: [ARCH] Thread safety

**Files:** `state.py`, `connection_manager.py`

| Item | Finding | Change |
|------|---------|--------|
| 5a | `state.py:39` — global `store = StateStore()` with exposed mutable dicts | Add mutator methods (`add_user`, `remove_match`, etc.) to `StateStore` |
| 5b | `connection_manager.py:48` — global `manager = ConnectionManager()` with mutable dicts, no lock | Add `threading.Lock` to `ConnectionManager` + protect all mutations |

### Batch 6: [ARCH] Documentation sync

**File:** `docs/ARCHITECTURE.md`

| Item | Finding | Change |
|------|---------|--------|
| 6a | Routes: docs reference `routes.py` but code has `routes_api.py`, `routes_html.py`, `routes_ws.py` | Update project tree, module descriptions, import structure, and all route references |
| 6b | State: docs describe module-level dicts but code has `StateStore` class | Update `state.py` section to document `StateStore`, `UserData`, `MatchData`, etc. |
| 6c | State re-export: docs claim `state.py` re-exports `CONVERSATION_PROMPTS` but it doesn't | Either add re-export or correct docs |
| 6d | DB schema: docs list 4 tables but code has 7 (`event_topics`, `user_interests`, `room_sample_users` missing) | Add all 7 tables to documentation |
| 6e | E2E tests: docs reference `userFlow.spec.ts` but code has `organizerFlow.spec.ts` + `participantFlow.spec.ts` | Update E2E section + add `globalSetup.ts` and `cleanup_e2e_db.py` |
| 6f | Test tree: 7 test files missing from docs listing | Add all to test tree |
| 6g | Config section: `DEFAULT_TOPICS` undocumented | Add to config constants |
| 6h | Helpers section: `insert_default_topics()`, `get_rooms_for_event()` undocumented | Add all 4 helpers |
| 6i | Utility tree: missing `filter_graph.py`, `enhance_graph_viewer.py` | Add to utility section |
| 6j | Function name: `find_match()` → `find_or_enqueue_match()` | Update in matchmaking section |
| 6k | File count: `test_frontend_files_exist` claims 32 files | Update to 44 |

## Files to Modify (complete list)

| # | File | Batches | Flag |
|---|------|---------|------|
| 1 | `app/sample_users.py` | 1 | [CLEANUP] |
| 2 | `app/connection_manager.py` | 1, 5 | [CLEANUP][ARCH] |
| 3 | `app/__main__.py` | 1 | [CLEANUP] |
| 4 | `app/helpers.py` | 2 | [CLEANUP] |
| 5 | `app/__init__.py` | 2 | [CLEANUP] |
| 6 | `app/handlers.py` | 2 — NEW | [CLEANUP] |
| 7 | `app/database.py` | 2 | [CLEANUP] |
| 8 | `app/routes_api.py` | 2, 3, 4 | [CLEANUP][ARCH] |
| 9 | `app/connection_service.py` | 4 — NEW | [ARCH] |
| 10 | `app/state.py` | 5 | [ARCH] |
| 11 | `app/matchmaking.py` | 4 | [ARCH] |
| 12 | `app/tasks.py` | 4 | [ARCH] |
| 13 | `frontend/src/hooks/useChipSelection.ts` | 3 — NEW | [CLEANUP] |
| 14 | `frontend/src/hooks/useDemoMode.ts` | 3 | [CLEANUP] |
| 15 | `frontend/src/hooks/useDemoSimulation.ts` | 3 — NEW | [CLEANUP] |
| 16 | `frontend/src/types/api.ts` | 3 | [CLEANUP] |
| 17 | `frontend/src/utils/demoData.ts` | 3 | [CLEANUP] |
| 18 | `frontend/src/pages/OrganizeEventPage.tsx` | 3 | [CLEANUP] |
| 19 | `frontend/src/pages/UserInfoPage.tsx` | 3 | [CLEANUP] |
| 20 | `frontend/src/pages/PeoplePage.tsx` | 3 | [CLEANUP] |
| 21 | `frontend/src/components/PersonCard.tsx` | 3 | [CLEANUP] |
| 22 | `docs/ARCHITECTURE.md` | 6 | [ARCH] |

## Testing Strategy
- Run full suite after EVERY batch: `npm test` (frontend 147/147), `uv run python tests/test_app.py` (backend), `npx tsc --noEmit` (TS)
- If test fails: classify via Failure Triage table (AGENTS.md) — fix import paths, adjust brittle tests, never change test logic
- If rename breaks imports: update ALL import sites in the same batch
- If extraction creates new file: write coverage test verifying extracted logic works independently

## Verification Gate Results

| Gate | Criteria | Status |
|------|----------|--------|
| G1 — Context | ARCHITECTURE.md read, all source files read, baselines confirmed | ✓ |
| G2 — Problem | All findings from both scans included, deduplicated, re-prioritized | ✓ |
| G3 — Goals | Measurable: same test counts, zero regression | ✓ |
| G4 — Approach | 6 sequential batches, one concern per batch, no conflicting batches | ✓ |
| G5 — Files | All 22 files identified with exact changes | ✓ |
| G6 — Tests | Full suite after every batch | ✓ |
| G7 — Risks | Moderate: renames affect multiple files (batch 3), new files need coverage tests | Noted |
